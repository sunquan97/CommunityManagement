server:
  port: 8080


spring:
  mvc:
    view:
      suffix: .jsp
      prefix: /WEB-INF/jsp
  #数据源  连接虚拟机
#  datasource:
#    url: jdbc:mysql://192.168.0.109:3306/CommunityManagement?characterEncoding=utf-8&serverTimezone=GMT
#    username: root
#    password: root1997
#    driverClassName: com.mysql.jdbc.Driver

  datasource:
    url: jdbc:mysql://localhost:3306/CommunityManagement?useUnicode=true&characeterEncoding=utf-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver


  #热部署
  devtools:
    restart:
      enabled: true  #设置开启热部署
      additional-paths: src/main/java #重启目录
      exclude: WEB-INF/**
      freemarker:
        cache: false    #页面不加载缓存，修改即时生效

thymeleaf:
  prefix: classpath:/WEB-INF/jsp
  suffix: .jsp

mybatis:
  type-aliases-package: com.pojo
  mapper-locations: classpath*:com/mapping/*.xml
  logging: #打印sql日志
    level:
    com.dao: debug

logging:
  file:
    name: CommunityManagement.log
  pattern:
    console: '%clr(%d{yyyy-MM-dd HH:mm:SS} [%thread] %-5level %logger{50} - %msg%n)'
    file: '%d{yyyy-MM-dd HH:mm:ss.SSS} >>> [%thread] >>> %-5level >>> %logger{50} >>> %msg%n'
#    path: logs



